<Window x:Class="Login.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Login"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBox x:Name="txtUsername" Grid.Row="0" Controls:TextBoxHelper.Watermark="Username" Margin="10"/>
        <TextBox x:Name="txtPassword" Grid.Row="1" Controls:TextBoxHelper.Watermark="Password" Margin="10"/>
        <Button Grid.Row="2" Margin="10" Click="btnLogin_OnClick">
            <Label Content="LOGIN" />
        </Button>
    </Grid>
</Window>
